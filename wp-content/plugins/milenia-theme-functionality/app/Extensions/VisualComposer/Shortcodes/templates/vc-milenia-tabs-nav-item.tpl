<span class="${panel_active_class}"><a id="tab-link-${panel_index}"
         href="#tab-${panel_index}"
         class="milenia-tab-link milenia-ln--independent"
         role="tab"
         aria-selected="${panel_selected}"
         aria-controls="tab-${panel_index}">${panel_title}</a></span>
